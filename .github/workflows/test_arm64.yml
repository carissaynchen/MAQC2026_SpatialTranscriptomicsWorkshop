name: Test ARM64 runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Check architecture
        run: |
          uname -m
          lscpu
          nproc
